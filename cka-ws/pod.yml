apiVersion: v1
kind: Pod
metadata:
  name: mypod
  labels:
    app: web
spec:
  containers:
    - name: web
      image: mtabishk/webserver-apache-php
      ports:
        - containerPort: 80
      resources:
        limits:
            cpu: 1
            memory: 1Gi
